<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <!-- 手动映射导入控制器（彻底替代@WebServlet注解）
    <servlet>
        <servlet-name>ImportTempController</servlet-name>
        <servlet-class>controller.ImportTempController</servlet-class>
    </servlet>
    <servlet-mapping>
        <servlet-name>ImportTempController</servlet-name>
        <url-pattern>/importTempController</url-pattern>
    </servlet-mapping> -->

    <!-- 可选：如果不用@WebFilter注解，可手动配置过滤器（二选一即可） -->
    <!--
    <filter>
        <filter-name>CustomCharacterEncodingFilter</filter-name>
        <filter-class>controller.CustomCharacterEncodingFilter</filter-class>
        <init-param>
            <param-name>encoding</param-name>
            <param-value>UTF-8</param-value>
        </init-param>
    </filter>
    <filter-mapping>
        <filter-name>CustomCharacterEncodingFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>
    -->

    <!-- 新增：默认访问页面配置 -->
    <welcome-file-list>
        <welcome-file>202408230118.html</welcome-file>
    </welcome-file-list>

    <!-- 欢迎页配置 -->
    <!-- <welcome-file-list>
        <welcome-file>temperature</welcome-file>
    </welcome-file-list> -->
</web-app>